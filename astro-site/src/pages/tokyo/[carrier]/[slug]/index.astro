---
import JapaneseShopPage from "../../../../components/JapaneseShopPage.astro";
import { loadTokyoShopPages, type LegacyShopPage } from "../../../../data/load-tokyo-shop-pages";

export function getStaticPaths() {
  return loadTokyoShopPages().map((page) => ({
    params: { carrier: page.carrier, slug: page.slug },
    props: { page },
  }));
}

type Props = { page: LegacyShopPage };
const { page } = Astro.props;
---

<JapaneseShopPage page={page} />
