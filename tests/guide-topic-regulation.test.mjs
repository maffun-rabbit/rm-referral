import test from 'node:test';
import assert from 'node:assert/strict';
import {validateSource} from '../scripts/page-sources.mjs';
const valid=()=>({locale:'ja',route:'topics/demo',pageType:'guide-topic-v1',title:'Demo',description:'Demo',styles:[],scripts:[],topic:{breadcrumb:{ariaLabel:'パンくず',home:'トップ',topics:'トピック',current:'デモ'},hero:{eyebrow:'GUIDE',heading:'見出し',lead:'結論',primaryActionLabel:'読む',sourceActionLabel:'出典',summaryLabel:'3点',points:['a','b','c']},sections:[{id:'answer',type:'prose',heading:'回答',paragraphs:['本文']}],sources:{eyebrow:'SOURCE',heading:'出典',description:'説明',updatedLabel:'確認日',items:[{label:'公式',href:'https://example.com'}]},updatedAt:'2026-09-21',cta:{eyebrow:'CTA',heading:'次へ',body:'説明',link:{label:'確認',href:'/'}},related:{eyebrow:'RELATED',heading:'関連記事',linkLabel:'読む',items:[{title:'関連',href:'/guide/'}]}}});
test('new guide topics require the structured regulation',()=>{assert.doesNotThrow(()=>validateSource(valid(),{isOverride:true}));assert.throws(()=>validateSource({...valid(),pageType:undefined,mainHtml:'<main></main>'},{isOverride:true}),/guide-topic-v1/);});
test('fixed guide topic areas cannot be omitted',()=>{const p=valid();delete p.topic.sources;assert.throws(()=>validateSource(p,{isOverride:true}),/source panel/);});
test('guide topics cannot add arbitrary layouts or local CSS',()=>{assert.throws(()=>validateSource({...valid(),mainHtml:'<main>custom</main>'},{isOverride:true}),/cannot define mainHtml/);assert.throws(()=>validateSource({...valid(),styles:['<style>main{}</style>']},{isOverride:true}),/cannot define page-local/);});
test('only approved variable section types are accepted',()=>{const p=valid();p.topic.sections[0].type='freeform';assert.throws(()=>validateSource(p,{isOverride:true}),/unsupported section type/);});
